#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#pragma warning(disable:4996)



int main(void) {

	char str[1000] = { NULL };
	int N = 0;
	scanf("%s", str);
	scanf("%d", &N);

	printf("%c", *(str + N-1));

	return 0;
}
