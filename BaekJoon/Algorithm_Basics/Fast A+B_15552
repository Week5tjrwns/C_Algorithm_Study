#include <stdio.h>
#pragma warning(disable:4996)


int main(void) {
	int T = 0;
	int A = 0;
	int B = 0;
	int count = 0;
	(void)scanf("%d\n",&T);
	while (count < T) {
		(void)scanf("%d %d",&A, &B);
		printf("%d\n",(A+B));
		count++;
	}


return 0;
}

