#include <stdio.h>

#pragma warning(disable:4996)
#define MAX 10001

int main(void) {

	int n=0, x = 0;
	static int array[MAX] = { NULL };

	(void)scanf("%d", &n);
	

	for (int i = 0; i < n; i++) {
		(void)scanf("%d",&x);
		array[x]++;

	}

	for (int i = 0; i < MAX; i++) {
		while (array[i]--) {
			printf("%d",i);
			puts("");
		}


	}


	return 0;
}
