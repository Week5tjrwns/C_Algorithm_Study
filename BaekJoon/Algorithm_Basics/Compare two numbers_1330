#include <stdio.h>

#pragma warning(disable:4996)


int main(void) {
	int A = 0;
	int B = 0;

	(void)scanf("%d %d",&A, &B);

	if ( A<-10000 || A>10000 || B>10000 || B<-10000 ){
	return 0;
}


if (A > B)printf(">");
else if (A < B)printf("<");
else printf("==");


return 0;
}

