#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#pragma warning(disable:4996)



int main(void) {
	int N = 0;
	scanf("%d", &N);
	static int arr[10000] = {0};
	char str[20] = {'0'};
	int top = -1;
	for (int i = 0; i < N; i++) {
		scanf(" %19[^\n]", str);
		
		if (strstr(str, "push")!=NULL) {
			arr[++top] = atoi((str + 5));
			
		}
		
		else if (strstr(str, "pop") != NULL) {
			if (top == -1)puts("-1");
			else printf("%d\n", arr[top--]);
		}
		
		else if (strstr(str, "size") != NULL)
			printf("%d\n", top+1);
		
		
		else if (strstr(str, "empty") != NULL) {
			if (top == -1)puts("1");
			else puts("0");
		}


		else if (strstr(str, "top") != NULL) {
			if (top == -1)puts("-1");
			else printf("%d\n", arr[top]);

		}

	
	}








	return 0;
}



